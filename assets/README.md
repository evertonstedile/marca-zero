# Assets

## Presentes
- `video/hero-bg.mp4` — fundo do hero 16:9 (re-encode `-g 5`, sem áudio, faststart)
- `video/hero-bg-9x16.mp4` — versão retrato para mobile (crop centrado no feixe de luz)
- `video/hero-poster.webp` — poster (frame 0)
- `teaser-marca-zero.mp4` — vídeo com a logo queimada · uso: teaser/OG/splash (NÃO usar no hero)

## Aguardando upload
- `logo-marca-zero.png` — logo principal em alta (o hero usa fallback tipográfico até chegar)
- `logo-metodo-raiz.png` — logo do método em alta

Os pontos que consomem as logos têm `<!-- PLACEHOLDER -->` no `index.html` e fallback automático.
